---
- hosts: vm 
  become: true 
#   gather_facts: no
  tasks:
   - name: copy app files
     copy:
      src: ./
      dest: /var/www/myapp

   - name: deploy app
     command: python3 /var/www/myapp/todo.py
